import { TextPaddingPipe } from './text-padding.pipe';

describe('TextPaddingPipe', () => {
  it('create an instance', () => {
    const pipe = new TextPaddingPipe();
    expect(pipe).toBeTruthy();
  });
});
