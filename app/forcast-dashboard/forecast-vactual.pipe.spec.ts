import { ForecastVactualPipe } from './forecast-vactual.pipe';

describe('ForecastVactualPipe', () => {
  it('create an instance', () => {
    const pipe = new ForecastVactualPipe();
    expect(pipe).toBeTruthy();
  });
});
