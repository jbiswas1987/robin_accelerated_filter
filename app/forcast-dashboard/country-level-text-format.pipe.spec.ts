import { CountryLevelTextFormatPipe } from './country-level-text-format.pipe';

describe('CountryLevelTextFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CountryLevelTextFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
